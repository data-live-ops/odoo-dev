<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Extend the partner form view -->
    <record id="view_partner_form_metabase_inherit" model="ir.ui.view">
        <field name="name">res.partner.form.metabase.inherit</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//group/group" position="inside">
                <field name="is_student" invisible="not is_student" readonly="1"/>
                <field name="is_parent" invisible="not is_parent" readonly="1"/>
            </xpath>
            
            <notebook position="inside">
                <page string="Parent Info" name="parent_info" invisible="not is_parent">
                    <group>
                        <group string="Parent Basic Information">
                            <field name="is_parent" invisible="1"/>
                            <field name="metabase_parent_id" readonly="1"/>
                            <field name="metabase_student_ids" string="Student User IDs" readonly="1"/>
                        </group>
                    </group>
                    <group name="parent_sync_info" string="Parent Sync Information">
                        <field name="metabase_last_sync" readonly="1"/>
                        <field name="metabase_sync_log_id" readonly="1"/>
                    </group>
                </page>
                <page name="child_info" string="Child Info" invisible="not is_parent">
                    <field name="student_parent_ids" readonly="1" mode="kanban" context="{'default_parent_id': id, 'default_street': street, 'default_street2': street2, 'default_city': city, 'default_state_id': state_id, 'default_zip': zip, 'default_country_id': country_id, 'default_lang': lang, 'default_user_id': user_id, 'default_type': 'other'}">
                        <kanban color="color">
                            <field name="color"/>
                            <field name="type"/>
                            <field name="is_company"/>
                            <templates>
                                <t t-name="card" class="flex-row">
                                    <aside class="o_kanban_aside_full">
                                        <field name="avatar_128" class="o_kanban_image_fill w-100" widget="image" options="{'img_class': 'object-fit-cover'}" alt="Contact image"/>
                                    </aside>
                                    <main class="ps-2 ps-md-0">
                                        <field name="name" class="fw-bold"/>
                                        <field name="function"/>
                                        <div t-if="record.metabase_user_id.raw_value">Student ID: <field name="metabase_user_id"/></div>
                                        <div t-if="record.metabase_grade.raw_value">Grade: <field name="metabase_grade"/></div>
                                        <field name="email" widget="email"/>
                                        <div t-if="record.type.raw_value != 'contact'">
                                            <div>
                                                <field class="me-1" name="zip"/>
                                                <field name="city"/>
                                            </div>
                                            <field class="me-1" name="state_id"/>
                                            <field name="country_id"/>
                                        </div>
                                        <div t-if="record.phone.raw_value">Phone: <field name="phone"/></div>
                                        <div t-if="record.mobile.raw_value">Mobile: <field name="mobile"/></div>
                                    </main>
                                </t>
                            </templates>
                        </kanban>
                        <form string="Student">
                            <sheet>
                                <field name="type" required="1" widget="radio" options="{'horizontal': true}"/>
                                <div class="text-muted oe_edit_only">
                                    <p class="mb-0" invisible="type != 'contact'">
                                        <span>Use this to organize the contact details of employees of a given company (e.g. CEO, CFO, ...).</span>
                                    </p>
                                    <p class="mb-0" invisible="type != 'invoice'">
                                        <span>Preferred address for all invoices. Selected by default when you invoice an order that belongs to this company.</span>
                                    </p>
                                    <p class="mb-0" invisible="type != 'delivery'">
                                        <span>Preferred address for all deliveries. Selected by default when you deliver an order that belongs to this company.</span>
                                    </p>
                                    <p class="mb-0" invisible="type != 'other'">
                                        <span>Other address for the company (e.g. subsidiary, ...)</span>
                                    </p>
                                </div>
                                <hr/>
                                <group>
                                    <group>
                                        <field name="name" string="Contact Name" required="type == 'contact'" placeholder="e.g. New Address"/>
                                        <field name="title" options="{'no_open': True}" placeholder="e.g. Mr." invisible="type != 'contact'"/>
                                        <field name="function" placeholder="e.g. Sales Director" invisible="type != 'contact'"/>
                                        <label for="street" string="Address" invisible="type == 'contact'"/>
                                        <div invisible="type == 'contact'">
                                            <div class="o_address_format" name="div_address">
                                                <field name="street" placeholder="Street..." class="o_address_street"/>
                                                <field name="street2" placeholder="Street 2..." class="o_address_street"/>
                                                <field name="city" placeholder="City" class="o_address_city"/>
                                                <field name="state_id" class="o_address_state" placeholder="State" options="{'no_open': True, 'no_quick_create': True}" context="{'country_id': country_id, 'default_country_id': country_id, 'zip': zip}"/>
                                                <field name="zip" placeholder="ZIP" class="o_address_zip"/>
                                                <field name="country_id" placeholder="Country" class="o_address_country" options="{&quot;no_open&quot;: True, &quot;no_create&quot;: True}"/>
                                            </div>
                                        </div>
                                    </group>
                                    <group>
                                        <field name="metabase_user_id" string="Student ID" readonly="1"/>
                                        <field name="metabase_grade" string="Grade" readonly="1"/>
                                        <field name="email" widget="email"/>
                                        <field name="phone" widget="phone"/>
                                        <field name="mobile" widget="phone"/>
                                        <field name="company_id" invisible="1"/>    <!-- Need to save value from parented record, cf onchange -->
                                    </group>
                                </group>
                                <group>
                                    <field name="comment" placeholder="Internal notes..." nolabel="1"/>
                                </group>
                                <field name="lang" invisible="True"/>  <!-- Need to add lang to save default value from parented record -->
                            </sheet>
                        </form>
                    </field>
                </page>
                <page string="Student Info" name="student_info" invisible="not is_student">
                    <group>
                        <group string="Student Basic Information">
                            <field name="is_student" invisible="1"/>
                            <field name="metabase_user_id" readonly="1"/>
                            <field name="metabase_school" readonly="1"/>
                            <field name="metabase_city" readonly="1"/>
                            <field name="metabase_notification_consent" readonly="1"/>
                        </group>
                        <group string="Student Additional Information">
                            <field name="metabase_grade" string="Grade" readonly="1"/>
                            <field name="metabase_curriculum" string="Curriculum" readonly="1"/>
                            <field name="metabase_lead_status" string="Lead Status" readonly="1"/>
                            
                        </group>
                    </group>
                    <group name="student_sync_info" string="Student Sync Information" invisible="not is_student">
                        <field name="metabase_last_sync" readonly="1"/>
                        <field name="metabase_sync_log_id" readonly="1"/>
                    </group>
                </page>
                <page name="guardian_info" string="Parent Info" invisible="not is_student">
                    <field name="student_parent_ids" readonly="1" mode="kanban" context="{'default_parent_id': id, 'default_street': street, 'default_street2': street2, 'default_city': city, 'default_state_id': state_id, 'default_zip': zip, 'default_country_id': country_id, 'default_lang': lang, 'default_user_id': user_id, 'default_type': 'other'}">
                        <kanban color="color">
                            <field name="color"/>
                            <field name="type"/>
                            <field name="is_company"/>
                            <templates>
                                <t t-name="card" class="flex-row">
                                    <aside class="o_kanban_aside_full">
                                        <field name="avatar_128" class="o_kanban_image_fill w-100" widget="image" options="{'img_class': 'object-fit-cover'}" alt="Contact image"/>
                                    </aside>
                                    <main class="ps-2 ps-md-0">
                                        <field name="name" class="fw-bold"/>
                                        <field name="function"/>
                                        <div t-if="record.metabase_parent_id.raw_value">Parent ID: <field name="metabase_parent_id"/></div>
                                        <field name="email" widget="email"/>
                                        <div t-if="record.type.raw_value != 'contact'">
                                            <div>
                                                <field class="me-1" name="zip"/>
                                                <field name="city"/>
                                            </div>
                                            <field class="me-1" name="state_id"/>
                                            <field name="country_id"/>
                                        </div>
                                        <div t-if="record.phone.raw_value">Phone: <field name="phone"/></div>
                                        <div t-if="record.mobile.raw_value">Mobile: <field name="mobile"/></div>
                                    </main>
                                </t>
                            </templates>
                        </kanban>
                        <form string="Student">
                            <sheet>
                                <field name="type" required="1" widget="radio" options="{'horizontal': true}"/>
                                <div class="text-muted oe_edit_only">
                                    <p class="mb-0" invisible="type != 'contact'">
                                        <span>Use this to organize the contact details of employees of a given company (e.g. CEO, CFO, ...).</span>
                                    </p>
                                    <p class="mb-0" invisible="type != 'invoice'">
                                        <span>Preferred address for all invoices. Selected by default when you invoice an order that belongs to this company.</span>
                                    </p>
                                    <p class="mb-0" invisible="type != 'delivery'">
                                        <span>Preferred address for all deliveries. Selected by default when you deliver an order that belongs to this company.</span>
                                    </p>
                                    <p class="mb-0" invisible="type != 'other'">
                                        <span>Other address for the company (e.g. subsidiary, ...)</span>
                                    </p>
                                </div>
                                <hr/>
                                <group>
                                    <group>
                                        <field name="name" string="Contact Name" required="type == 'contact'" placeholder="e.g. New Address"/>
                                        <field name="title" options="{'no_open': True}" placeholder="e.g. Mr." invisible="type != 'contact'"/>
                                        <field name="function" placeholder="e.g. Sales Director" invisible="type != 'contact'"/>
                                        <label for="street" string="Address" invisible="type == 'contact'"/>
                                        <div invisible="type == 'contact'">
                                            <div class="o_address_format" name="div_address">
                                                <field name="street" placeholder="Street..." class="o_address_street"/>
                                                <field name="street2" placeholder="Street 2..." class="o_address_street"/>
                                                <field name="city" placeholder="City" class="o_address_city"/>
                                                <field name="state_id" class="o_address_state" placeholder="State" options="{'no_open': True, 'no_quick_create': True}" context="{'country_id': country_id, 'default_country_id': country_id, 'zip': zip}"/>
                                                <field name="zip" placeholder="ZIP" class="o_address_zip"/>
                                                <field name="country_id" placeholder="Country" class="o_address_country" options="{&quot;no_open&quot;: True, &quot;no_create&quot;: True}"/>
                                            </div>
                                        </div>
                                    </group>
                                    <group>
                                        <field name="metabase_parent_id" string="Parent ID" readonly="1"/>
                                        <field name="email" widget="email"/>
                                        <field name="phone" widget="phone"/>
                                        <field name="mobile" widget="phone"/>
                                        <field name="company_id" invisible="1"/>    <!-- Need to save value from parented record, cf onchange -->
                                    </group>
                                </group>
                                <group>
                                    <field name="comment" placeholder="Internal notes..." nolabel="1"/>
                                </group>
                                <field name="lang" invisible="True"/>  <!-- Need to add lang to save default value from parented record -->
                            </sheet>
                        </form>
                    </field>
                </page>
                <page name="sync_log" string="Sync Log" invisible="not is_student and not is_parent">
                    <field name="metabase_sync_log_ids" mode="list" readonly="1">
                        <list default_order="name asc">
                            <field name="name"/>
                            <field name="create_date"/>
                            <field name="start_date"/>
                            <field name="end_date"/>
                            <field name="total_records"/>
                            <field name="created_count"/>
                            <field name="updated_count"/>
                            <field name="sync_type"/>
                            <field name="state" decoration-success="state == 'done'" 
                                decoration-danger="state == 'failed'"
                                decoration-info="state == 'processing'"/>
                        </list>
                    </field>
                </page>
            </notebook>
        </field>
    </record>
    <record id="view_res_partner_filter_student_parent" model="ir.ui.view">
        <field name="name">res.partner.select.student.parent</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_res_partner_filter"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='name']" position="after">
                <separator/>
                <field name="metabase_user_id" string="Student ID"/>
                <field name="metabase_parent_id" string="Parent ID"/>
            </xpath>
        </field>
    </record>
</odoo>
